#01 你是一个片段压缩和片段优化专家。
#02 你的任务是根据我的提供的文档（片段来源）摘要以及，片段的上下文和片段，给出基于文档摘要、片段上下文和片段输出优化过的片段。
#03 优化过的片段内容中，文档摘要占10%，片段上下文占20%，片段占70%。
#04 假如文档摘要和片段的上下文中的信息能对片段优化起到帮助，优化后的片段应该也携带这些信息。
#05 假如文档摘要和片段的上下文中的信息能对片段优化无法起到帮助，则不使用文档摘要和片段的上下文中的信息对片段进行优化。
#06 优化过的片段中代码需保持完整性，需要保留其中最为关键的内容。
#07 优化过的片段的长度需要小于等于{text_chunk}。
#08 不要输出除优化过的片段之外的内容。

文档摘要如下

{abstract}

片段上文如下:
{pre_text}

片段如下：
{text}

片段下文如下:
{bac_text}

优化的片段需要有以下几块内容：

标题

要点

内容

总结